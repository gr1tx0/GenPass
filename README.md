# GenPass
# 🔐 Генератор надёжных паролей (PassGenerator)

Простая, **автономная консольная утилита на C++**, которая генерирует криптостойкие пароли **без подключения к интернету** и **без сохранения данных**. Идеально подходит для повседневного использования и обучения основам информационной безопасности.

---

## ✨ Возможности

- Генерация паролей заданной длины (минимум 4 символа)
- Использование всех безопасных классов символов:
  - строчные буквы (`a–z`)
  - заглавные буквы (`A–Z`)
  - цифры (`0–9`)
  - специальные символы (`!@#$%^&*()_+-=[]{}|;:,.<>?`)
- Опциональное **исключение похожих символов** (`0/O`, `1/l`, `I/l`), чтобы избежать ошибок при ручном вводе
- Проверка: содержит ли пароль все 4 типа символов (с предупреждением, если нет)

> 💡 Программа **не гарантирует** наличие всех типов символов в каждом пароле — но сообщает об этом. Для максимальной надёжности можно перегенерировать.

---

## 🛡️ Безопасность

- 🔒 **Полностью оффлайн** — не отправляет данные в сеть
- 🧼 **Не сохраняет** пароли на диск
- 🚫 **Не требует** прав администратора
- 🧪 **Нет внешних зависимостей** — всё работает на стандартной библиотеке C++

> Программа собрана с **статической линковкой** (`/MT`), поэтому запускается на любом Windows 10/11 без установки Visual Studio или Redistributable.

---

## ▶️ Как использовать

1. Скачайте `PassGenerator.exe` из [Releases](https://github.com/gr1tx/PassGenerator/releases)
2. Запустите файл двойным кликом
3. Следуйте инструкциям:
   - Укажите длину пароля (например, `12`)
   - Выберите, исключать ли похожие символы (`y`/`n`)
4. Скопируйте сгенерированный пароль

### ⚠️ Предупреждение SmartScreen

При первом запуске Windows может показать:

> _«Windows защищает ваш компьютер»_ → _«Издатель не подтверждён»_

Это **нормально** для самописных программ. Нажмите:
1. **«Подробнее»**
2. **«Выполнить в любом случае»**
